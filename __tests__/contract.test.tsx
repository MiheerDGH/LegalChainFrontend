import React from 'react';
import { render } from '@testing-library/react';

describe('contract smoke', () => {
	it('sanity check', () => {
		// simple smoke to ensure test runner picks up the file
		expect(true).toBe(true);
	});
});
